# CPSC 542 Assignment 2 pipeline (train + visualize)
# Install: pip install -r requirements.txt
# PyTorch: if the default wheel fails on your machine, use the selector at https://pytorch.org/get-started/locally/

torch>=2.0
torchvision>=0.15
numpy>=1.20
pandas>=1.3
Pillow>=9.0
matplotlib>=3.5
scikit-learn>=1.0
